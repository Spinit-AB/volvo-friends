import type { NextConfig } from "next";

// TODO: Add image optimization config for Sanity CDN
// TODO: Consider adding remotePatterns for cdn.sanity.io
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
